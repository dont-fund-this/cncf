type address = string
type payload = string
type options = string

type cabi = {
  name: string;
  path: string;
  pump_fn: nativeint;
}

type triplet = {
  address: string;
  payload: string;
  options: string;
}
